pre-commit:
  parallel: true
  jobs:
    - name: biome-check
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: corepack pnpm exec biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
