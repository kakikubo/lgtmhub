# 設計: glossary.md の物理クリーンアップ参照を「機能8」に修正

## 方針

`docs/glossary.md` の4箇所のみを書き換える。コード・テストへの影響はない。
表記は `docs/functional-design.md:344`（`PRD機能8`）と `docs/glossary.md:168`（`PRD 機能8`）が
既存の正しい例だが、Issue の対応案に従い **`PRD 機能8`**（中黒スペースあり）へ統一する。

## 変更対象

| 行 | 変更前 | 変更後 |
|---|---|---|
| 155 | `P1 機能9（削除画像の物理クリーンアップ）` | `PRD 機能8（削除画像の物理クリーンアップ）` |
| 514 | `（終端、30日後にBlob物理削除：PRD機能9）` | `（終端、30日後にBlob物理削除：PRD 機能8）` |
| 523 | `deleted --> [*]: 30日後に物理削除（P1機能9）` | `deleted --> [*]: 30日後に物理削除（PRD 機能8）` |
| 555 | `物理クリーンアップ（PRD機能9）の判定基準` | `物理クリーンアップ（PRD 機能8）の判定基準` |

168 行目の `**参考**: [PRD 機能8](./product-requirements.md)` は既に正しいため変更しない。

## 検証方法

1. `grep -n '機能9' docs/glossary.md` → 0 件
2. `grep -rn '機能8\|機能9' docs/` → 残る参照がすべて PRD の採番と一致
3. `pnpm run check`（Biome）— markdown は対象外だが差分がないことを確認
4. mermaid ブロックの構文が壊れていないこと（ラベル内に特殊文字を追加しない）

## リスク

- なし（ドキュメントのみ）。mermaid ラベルは全角括弧のままなので構文影響なし。
